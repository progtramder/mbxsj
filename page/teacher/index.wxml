<view class="container">
  <view class="page-body">
    <view wx:for="{{teacher}}" wx:key="item" class="exhibit-item-wrapper">
      <exhibit-item url="appointment/index?id={{item._id}}&name={{item.name}}"
        imgshape="circle"
        imgpath="{{item.anvatar}}"
        title="{{item.name}}"
        body="{{item.introduction}}"
        footer=""
      />
    </view>
  </view>
</view>